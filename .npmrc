# @cloudflare/next-on-pages peers @cloudflare/workers-types@^4, while current
# wrangler peers ^5. The two cannot be satisfied together, so npm's strict peer
# resolution fails the Cloudflare Pages build. Neither package is a real
# dependency of this app, so relaxing resolution is safe here.
legacy-peer-deps=true
